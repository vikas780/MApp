import 'package:flutter/material.dart';

class ManagementApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Management App",textDirection: TextDirection.ltr,),
    );
  }
}
